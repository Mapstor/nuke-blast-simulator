'use client'

// PopulationIndicator is a placeholder. The original implementation referenced
// `populationData` and `detonationPoint` fields that never existed on the
// simulator store, so the component has been a runtime no-op since launch.
// Will be re-implemented when population data flows into the store
// (planned Week 2 — see HUDOverlay / ResultsPanel for current density UX).
export function PopulationIndicator() {
  return null
}
